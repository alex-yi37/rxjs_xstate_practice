import { createMachine } from "xstate";
import { useMachine } from "@xstate/react";

import { simulatedFetch } from "@/utils";

export const XStateApiValidatedInput = () => {
  return <h1>xstate validated input</h1>;
};
